<h1><%= @user.id ? 'Edit User' : 'Create User' %></h1>
<hr />
<%= form_for @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>